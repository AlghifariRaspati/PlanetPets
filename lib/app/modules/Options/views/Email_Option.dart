import 'package:flutter/material.dart';

class EmailOption extends StatelessWidget {
  const EmailOption({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
